<%= simple_form_for([@assembly, @participant]) do |f| %>
  <% if @participant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@participant.errors.count, "error") %> prohibited this assembly from being saved:</h2>

      <ul>
      <% @participant.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="inputs">
    <%= f.input :name %>
    <%= f.association :user %>
    <%= f.input :roles %>
    <%= f.input :description %>
    <%= f.input :login %>
    <%= f.input :password %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
